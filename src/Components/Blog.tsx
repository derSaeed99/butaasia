import { Grid, Typography } from "@mui/material";
import React from "react";

export const Blog = () => {
  return (
    <Grid container display="flex" alignItems="center">
      <Grid item xs={12}>
        <Typography variant="h5">Bald verfügbar</Typography>
      </Grid>
    </Grid>
  );
};
